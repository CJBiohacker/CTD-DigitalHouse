let formulario = document.getElementById('form');
let textoSaida = document.getElementById('comentario-saida');

const capturaComentario = (comentario) => {

    localStorage.getItem(evento);
    comentario.preventDefault;

};

formulario.addEventListener('submit', capturaComentario());

